# TODO

- projects page
- bookshelf page
- change light and dark theme colors (to kishan and my old theme colors)
- seo / link previews (all in `_config.yml`)
  - `description:` is still the al-folio demo blurb ("A simple, whitespace theme for academics"). this is the grey text google shows under the site in search results
  - `serve_og_meta: false`, so pasting the site into whatsapp / linkedin / x gives a bare url with no preview card
  - `og_image:` is empty, that is the thumbnail for the preview card. `profile_pic.jpg` would do
  - `serve_schema_org: false`, structured data telling google "this page is a person, Shardul Junagade, IIT Gandhinagar"
